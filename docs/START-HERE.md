# START HERE — Nova beta pack (paste to Claude Code)

1. Unzip this pack into the dance-project repo root: skills/ → .claude/skills/ ; all .md files → docs/ (or root).
2. Then paste to the CLI:

   Read GAMES-TIERED.md. Run session S1 (install skills + CLAUDE.md + beta track + guardian + rollback test) and S2 (SKILL-CHECK audit of Freeze + Upper Body against the 3 skills). Paste the SKILL-CHECK table. HOLD.

3. After the founder approves the fix list → sessions S3-S7, one per version (b0.2 … b0.6), each ends with a recording + 3 beeps.
4. Nothing reaches main without "PROMOTE vX.Y".
