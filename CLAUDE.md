# CLAUDE.md — Nova repo laws (auto-loaded every session)

1. Read CONTEXT.md first, then the skill for the job in .claude/skills/ (movement-tracking, game-cues-lights, performance-feedback-ai, game-vfx-juice, lexi-ux-patterns, ai-companion-presence).
2. All game work lands on branch `beta`. `main` is frozen: only PRs titled `PROMOTE vX.Y` (freeze files: `FREEZE-APPROVED` in the body). Tags `beta-b0.x-<name>`.
3. Build games ON the shared engines (pose-engine, mover-engine + rules, light-engine, nova-bridge, tiers) — never re-implement detection or lights inside a page. Template = beta/wave.html.
4. Laws: INPUT-LOCK · phase machine (lips only on the calm body; game voice in the air; holds mute) · live V2V voice only · truth-gate · consent gate · never negative · clock law · body map · no silent fallbacks (missing asset = red banner).
5. Every value is cited (a spec in docs/, ORIGINS, or measured) or marked QUESTION. A real gap you fill = `[CLI-FILL] what & why` in the report.
6. One bounded task per session. Evidence: logs + screenshots + a recording to Downloads + 3 beeps. Then HOLD for the founder.
7. Pods: Secure 4090, one pod per volume, watchdog self-stop, check RunPod balance before blaming capacity. Never touch Maya pods/files.
8. Bakes are treasure: never delete; use the Bake Factory only.
