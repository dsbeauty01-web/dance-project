# MAYA — STATUS (2026-08-25) · Work Order v2 execution
Branch `maya-p1-finish` (off `maya-p1-ops`). Did every task that needs **no pod** and **no serum clip**; held the rest for the human, as instructed.

## ⛳ OPEN QUESTION FOR THE HUMAN (answer this first)
- **rapa: hype dance beat, or a scored GAME?** If it's a game, I need the **win condition** to write the move-detection cues. (Language is already answered: **English**.)

---

## Per-task status
| Task | Status | Notes |
|---|---|---|
| **T1 Security** | 🟡 DONE (no-pod parts) / NEEDS-POD | Repo + history re-scanned **clean** (only hit = Google Sheet ID, not a secret). Escalated **RunPod key → exposed** (plaintext in `bake_master.sh` on the volume). Updated `security/ROTATION_CHECKLIST.md` + expanded `security/boot.env.example` (now covers RunPod + Hume + bake scripts). **Volume-script refactor** (strip inline keys from `boot.sh`/`bake.sh`/`bake_master.sh` → source `.env`) is deferred to the next pod session. |
| **T2 Serum QC** | 🔴 BLOCKED | **No serum video clip exists yet** — Downloads has serum *PNG stills* only, no Kling "PRESENT" clip. Nothing to QC/bake. QC harness pattern is ready (frame-extract like the rapa work) the moment a clip lands. |
| **T3 Bake `maya_serum`** | 🔴 BLOCKED | Needs the serum clip (T2) **and** a pod session. |
| **T4 One pod session (batch)** | ⏸ HELD | Needs your go + the serum clip. Will batch: `maya_rapa` English MuseTalk test → bake `maya_serum` → serum test → verify rapa⇄serum switching → evidence → stop pod. |
| **T5 English pass** | 🟢 DONE | `serum_lines_EN.md` (catalog facts only, **no medical claims**, 6 Q&A + guardrails), `serum-c.en.json`, staged **W4 English system-prompt** (`W4-seller-prompt-EN.md`, *not activated*), `MAYA-RAPA-DANCE-PROMPT-PLAN_EN.md`. Hebrew untouched. |
| **T6 Live wiring** | 🟡 DONE (no-pod parts) | `maya-golive.mjs` now takes an **avatar param `rapa\|serum`** + does a `/set_avatar` step (honest UNTESTED on Maya pod). `maya-godark.mjs` already complete. Meta pack **verified complete** (icon present, no placeholders). `DEMO_RUNBOOK.md` updated for **English + rapa⇄serum switching + serum host**. OBS cutaway import deferred (no approved non-bake clips yet). |
| **T7 Report** | 🟢 DONE | This file. |

## Pods / cost
- **This work-order session used NO pod** — all tasks were no-pod. **0 pods running now** (45 total, all EXITED).
- (Reference: the earlier `maya_rapa` bake session ran ~40 min ≈ **$0.45**, already stopped.)

## The single human-click list
1. **Rotate keys (urgent):** revoke + mint new **RunPod** and **OpenAI** (freshly exposed on the volume); then GitHub PAT, n8n, Meta, LiveKit, Hume. Details per key in `security/ROTATION_CHECKLIST.md`.
2. **Generate the serum "PRESENT" Kling clip** (Maya holding the bottle, ~15s, first frame = last frame, one continuous take, mouth closed) → drop in Downloads. This unblocks T2/T3/T4.
3. **Answer the open question:** rapa = hype beat or scored game (+ win condition).
4. **Give the go for the pod session** (T4) once #2 is in.
5. **Meta:** upload icon, set Category = Business, click Submit (pack is ready in `meta-review/PASTE_ME.md`).
6. **OBS first run** on the demo machine (`obs64.exe --startstreaming --minimize-to-tray`).

## Shortest path to a client-facing selling demo
**serum clip → one pod session (bake `maya_serum` + verify rapa⇄serum switch + English lip-sync) → point `Fetch catalog` at `serum-c.en.json` + activate the EN W4 clone → dry-run the DEMO_RUNBOOK.** Everything except the serum clip and the pod session is staged and waiting.

---

# SERUM BAKE ORDER — progress (appended 2026-08-25)

| Step | Status | Notes |
|---|---|---|
| **S1 Prep + QC** | 🟢 DONE | 4 clips QC'd + re-encoded to 1080p/24fps/-an masters → `maya-ops/bake/src/` (gitignored; manifest in `bake/README.md`). Both bake sources **BAKE-READY** — mouth closed, no ghost/double-face, bottle stable, no trim needed. `serum_present` is the strong one (frontal, medium-large face — should lip-sync *better* than rapa). |
| **S2 Pod bake** | ⏸ HELD (needs pod + your go) | Bake `maya_serum` from `serum_present_src.mp4`; optional `maya_serum_close`; + run the pending `maya_rapa` English talk-test in the same session. |
| **S3 Cutaways + reject** | 🟢 DONE | `cutaway_examine` / `cutaway_apply` masters ready; `demo/OBS-CUTAWAYS.md` documents CUT_EXAMINE / CUT_APPLY + the "voice keeps talking over B-roll" rule. Rejected male-hand clip → `maya-ops/bake/rejected/`. |
| **S4 Report** | 🟢 DONE (this section) | maya_serum frames/latents = pending pod. Cost so far this order: **$0 (no pod used)**, 0 pods running. |

### ⚠️ New flag from the serum QC
The serum host is a **photorealistic woman** — a totally different look from the 3D-cartoon `maya_rapa`.
rapa ⇄ serum switching would jump cartoon ↔ real person. Not a blocker; confirm it's intended.

### Serum order — remaining human clicks
- Give the **go for the pod session** (batches: bake `maya_serum` + rapa/serum English lip-sync tests + switching check).
- (No new key clicks beyond the v2 list; Meta submit still open.)

---
*Commits on `maya-p1-finish`: T1 security · T5 English · T6 wiring · T7 report · serum S1/S3. Push at end of session.*
