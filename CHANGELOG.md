# CHANGELOG — Nova

Format: one line per beta version + every promotion to main.

- b0.21-novasays — NOVA SAYS built from scratch on the engines (NOVA-SAYS.md, novapack7): beta/novasays.html (her 60/kid 40, phase machine, presence gate, HUD with round dots + 🕵️ tricks counter, stars cards, scorecard finale) + beta/novasays.js (pure library: 12-command CMDS with EN+HE lines/keys, 4-round sequencer incl. YOU'RE-THE-BOSS, never-negative scoring, boss parseCommand EN+HE) + pod /beta/novasays + /beta/novasays.js routes. STEP-0 gesture map from the 2026-08-18 avatar audit: armsUp=gest_star, leftUp=gest_lefthand, rightUp=gest_righthand, clap=gest_clap, freeze=gest_bear, wave=nova_wave_a; head/hips/knees/jump/headSide/shoulders NOT baked → spoken+lit only (demo:null). Engines: mover-rules +handsOnHead/handsOnHips/touchKnees (spec-verbatim thresholds); light-engine +gotchaRipple (purple #b98cff chest ring)/softFade/clearAll — NOTE these engine additions physically landed inside the Lights CLI's concurrent b0.22 commit (1dcec58, it committed while they were staged); content verified present. [CLI-FILL] ARM GATE: bench measured resting wrists ≈0.10 from hips < spec's 0.14 → raw handsOnHips fires on a motionless kid; spec value untouched, page requires one frame of real wrist motion (clap's 0.8 mag) before a proximity hit — QUESTION logged for the founder. test/novasays_bench.js 67/67 (rules, sequencer invariants, scoring fuzz ×2000, boss EN+HE, static kids-law graders) + test/novasays_smoke.js 10/10 (full 4-round headless session: score 700, 7 tricks caught, tricks never lit, PULSE fired). Evidence: Downloads/NOVASAYS-*.png + NOVASAYS-SMOKE-RUN.webm + NOVASAYS-SMOKE-LOG.json. Tag beta-b0.21-novasays.
- b0.20 — FREEZE-FIX-GROOVE-START (FREEZE-APPROVED): she grooves from the FIRST beat. Founder-observed bug: music started on the calm idle body and the groove only appeared at freeze #1's release. Fix in beta/freeze.html + pod/pages/animal-freeze.html: (1) setPhase(GAME) now GUARANTEES the groove body — any path into GAME sets nova_idlegroove_v2 if the body isn't already groove (defense in depth; no-op on the per-round endFreeze→GAME hop so NEUTRAL's loop clock stays honest); (2) the switch point calls setPhase(GAME) BEFORE Music.start() with a 300ms lead (the /set_avatar path engine→bridge→LiveKit is async), Flash.fire() masking the idle→groove cut — so `[BODY] nova_idlegroove_v2 phase=game` logs before the first [FREEZE] and she is dancing ON beat one; (3) GROOVE_BODY named once next to TALK_BODY. Evidence pending: one playthrough log showing the [BODY] order + a 10s intro→music recording (founder hold). Tag beta-b0.20.
- b0.19 — Wave game on the accurate comet: beta/wave.html rewired to the phase-driven comet — the repeating-timer guide is gone; cometLive(waveR/waveL) starts at game start and rides whichever arm actually waves; the cue previews the path once (cometCue); a detected wave adds the fingertip overshoot (cometHit); the hold is now the ICE GLOW (cometStop + freezeStart/Break/End, no ring/orb). Both wave rules are fed EVERY pose frame so phase() has history. attachNova wrapped in try/catch so the game runs headless/lights-only when the pod is down. New beta/wave-test.html — a self-driving harness (mover_bench-proven driven waves: shoulder→elbow→wrist→index dips, all four peak so check() scores) proving comet + scoring + freeze end-to-end: score climbs 0→180→230, ice glow on the hold. Downloads/wave-game-test.mp4. Tag beta-b0.19.
- b0.18 — WAVE-COMET-ACCURATE (novapack4): the wave comet now rides the REAL detected wave, not a timer. WaveRule.phase() exposes the live wave head (0..1 along the arm, following the most recent joint peak, easeOutCubic glide ~220ms/link). light-engine comet rebuilt: cometCue (path sweep), cometLive(rule,chain) (phase-bound, one state per arm), cometHit (260ms fingertip overshoot/follow-through), cometStop; a tapered Catmull-Rom "sleeve of light" ribbon (not dots); PATH GUARD (no comet on a bunched/short arm → soft wrist pulse only); head radius capped at 0.16×sw (the v4 "ball" bug is dead); gaussian sparks. CRITICAL FIX: WaveRule.push() now dedupes same-timestamp entries — check() and phase() both push per frame, and the duplicates were silently killing peak detection (would have broken the live comet AND partially the detector). New test/wave_phase_bench.js 5/5 (head sweeps 0→1 with the peaks); mover_bench still 11/11. NOTE: game-vfx-juice SKILL.md is MISSING [CLI-FILL logged] — used the spec's embedded QA checklist instead. handywave.mp4 turned out to be a dancing-avatar idle (arms held out / off-frame), NOT an actual arm-wave, so the path guard correctly shows no comet on it; the mechanic is proven by the unit test + a synthetic-wave visual. Tag beta-b0.18.
- b0.17 — LIGHTS-FINAL (novapack4): the founder-approved light LANGUAGE, one distinct effect per move. WAVE = the COMET (glowing head + fading tail + sparks riding the arm; dim/slow cue vs bright/fast hit; cometCue/comet). RIBS/TORSO/HIPS = the HOOP (perspective ring that slides for side / swells F / lights B + a dashed white hip hoop that never moves; hoopCue/hoopArc/drawHoopCue/hoopHit). FREEZE = just the ICE GLOW on the body silhouette, faint→deep through the hold — segmentation mask when available (setMask/tint), soft-ellipse fallback otherwise; no ring/specks (freezeStart/freezeBreak/freezeEnd, legacy freezeCue/Held/Broke aliased). CLAP = the SNAP (hand glows + thin line; contact = a 4-point star + one thin 250ms ring; clapCue/drawClapCue/clapHit/drawSnaps). bloom() reduced to a single soft additive blob (layered by repeated calls); draw order cues-by-type→comets→freeze→parts→rings→snaps. New beta/lights-demo-v4.html previews the language on the real body, WAVE first. Downloads/lights-demo-v4.mp4. NOTE: hoop+freeze read great on the canned demo body; wave+clap are correct but need a body that performs them (the live game). Tag beta-b0.17.
- b0.16 — LIGHTS-LOOK (novapack4): shared/light-engine.js visual pass — every size scaled to live shoulder-width (orb 0.30×sw, ring 2.0×orb, labels capped), 3-layer additive BLOOM on all gold + 7 orbiting sparkles per orb, PERFECT zone = a filled translucent gold BAND, feathered rings (shadowBlur). COMET = bloomed head + 18-fade tail + shed sparks. LABELS ride ABOVE the head (face = exclusion zone), size FROZEN at birth so shot-cuts never balloon them. FREEZE = frosty ice glow hugging the torso + drifting frost + thick feathered closing ring, FROZEN! outline flash. CLAP = bloomed wrist orbs + glowing pulsing dotted bridge + contact bloom with 8 radial spokes. Adult tier = amber, particles ×0.4, still bloomed. All fades/growth/rises are now WALL-CLOCK (born+life, dt-scaled motion) so it looks identical at 10fps capture and 60fps device. New beta/lights-demo-v3.html re-records on the real body. Downloads/lights-demo-v3.mp4. Tag beta-b0.16.
- b0.14 — LIGHTS FEEDBACK (game-cues-lights): CLAP now reads as a clap — orbs on BOTH wrists + a dotted "bring together" bridge, window ring at the midpoint, and on contact the burst + expanding ring happen AT the point between the hands, with a real clap transient (filtered noise, not the sine blip). FREEZE gets a detection window — an ice ring around the whole body that CLOSES over the hold = the stay-still countdown; solid + "FROZEN!" flare on success, warm fade if broken. DETECTION WINDOW everywhere: every closing ring also marks the PERFECT zone (brighter inner third), the grade word (PERFECT/GOOD/OK) floats up on a hit, and a window that closes with no hit just soft-fades (no red, no penalty). New `beta/lights-demo-v2.html` re-records the demo on a REAL body — live MediaPipe pose detection driving the lights on a full dancing figure (not the stick figure). Downloads/lights-demo-v2.mp4.

## main
- 2026-09-10 — PROMOTE v1.0.2 — groove from beat one: setPhase(GAME) guarantees the groove body + 300ms /set_avatar lead before Music.start() — Nova grooves from beat one (from beta-b0.20, founder-confirmed on the live pod). PR #46, tag v1.0.2.
- 2026-09-06 — Hebrew across the whole game (lang persistence, Hebrew game cues, say-translation, sticky-Hebrew lock). PR #44.
- 2026-09-05 — Tester package: locked GitHub-Pages URL + pod auto-detect, waking-up state, parent consent gate (HE+EN), one-tap boot/schedule. PR #41/#42.
- 2026-09-03 — RELEASE: Freeze game machine-certified EN + HE (3 clean sessions/lang). PR #39.

## beta
- b0.15 — WAVE-FULL: beta/wave.html built on the shared engines (mover b0.11 + light b0.12 + pose b0.10). 28.5s video-clock timeline (R×3 → L×3 → both×2 → 1.5s hold), adult tier, comet light on the arm chain, iso-scored. New shared/nova-bridge.js (page↔pod live bridge — attachNova/setBody/routeVoice/note/onHerAudio + nova:consent, extracted from the freeze Live object) + shared/pulse.js + startPose() export + WaveRule.lastResult. pod: /beta/wave route + /media static. HE via ?lang=he. QUESTION resolved: no separate wave music in repo → handywave.mp4's own audio IS the track. Detection proven offline (mover_bench 11/11, wave smooth+iso). LIVE-VOICE bridge + graders/recording PENDING a pod. Tag beta-b0.15.
- b0.13 — Up Groove game logic on the two engines (beta/upgroove-game.js): the traced ladder (head→shoulder→ribs→hips → double → chain → freestyle) → mover-rules detection + graded windows + kids-tier scoring (100/120/140/130, iso bonus, streak ×2) + gold lights. test/upgroove_bench.js 8/8.
- b0.12 — shared LIGHT engine (shared/light-engine.js): gold-only orb/ring/ribbon/comet/flare/fly-up/ice/warm on live joints, kids/adult tiers, never red. beta/lights-demo.html (30s scripted showcase) + recorder → Downloads/lights-demo-video.mp4.
- b0.11 — shared DETECTION engine: shared/mover-engine.js + mover-rules.js + cue-window.js on pose-adapter; test/mover_bench.js proves every rule fires only on its clip (freeze/shoulderPop/ribSlide/hipSlide/clap/wave), iso correct, clap-audio fusion, wave quality; 11/11, ~30k updates/s.
- b0.10 — DETECTION: MediaPipe Pose Landmarker engine + adapter on Freeze beta (shared/pose-engine.js, GPU→CPU→MoveNet fallback, self-hosted model). Stillness detector reads named joints via toNova only (grep-proof). Recal harness: certified 0.045 still-threshold holds (MoveNet→MediaPipe shift +14%, within skill's 10-20%). Real-engine fake-camera run + ?pose=1 overlay captured. Fixed two latent bugs (dead window.Pose guard — also on main animal-freeze.html, reported; <base>-tag import mis-resolution). G6 body-map grader PASS on the live pod; G1/G4/G5 remain the pre-existing brain-tuning failures (out of scope for a detection session).
- b0.9 — Wave switched to the ADULT coach tier (calm/precise, ≤10 words, technical praise, external corrections) — was wrongly on the kids voice.
- b0.8 — Up Groove feedback-law block (4:1 ratio, external corrections, no trait praise).
- b0.7 — shared tier config (beta/tiers.js); real Up Groove/Wave synced into beta.
- b0.6 — movement-tracking A1: Upper Body stops SCORING front/back (depth, unjudgeable in 2D) — F/B is now an unscored vibe beat, never a miss.
- b0.5 — movement-tracking A3/A4: Freeze per-kid calibration window + stronger gate (0.35, 8 joints, EMA) + absent kid = NOSHOW not a miss.
- b0.1/S1 gap-fill (DEPLOY-TEST-ORDER) — movement-tracking skill → v2 (MediaPipe map, v1 kept as SKILL-v1.bak); `shared/pose-adapter.js` (skill §2 verbatim); `tools/beta-init.sh` recorded + run (new beta copies: wave, hello, upgroove); deploy-test docs → docs/; rollback.sh tested on beta-b0.1 in a clean clone (exit 0).
- b0.4 — game-cues-lights B3/B6: Upper Body cue TEXT chases the gold (not "hands on your waist"); Freeze gains an always-visible Exit button.
- b0.3 — game-cues-lights B5: Freeze's punishing RED moving-ring → soft warm amber (never red).
- b0.2 — performance-feedback-ai C1/C2/C3 + B3: feedback-law block into both game brains (4:1 encourage:correct, specific/malleable praise not trait, external-focus target cues not body parts, future-tense corrections).
- b0.1 — beta track opened from main; skills installed; guardian + rollback in place.
